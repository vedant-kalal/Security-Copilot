# The extension reads its API base URL from chrome.storage (configured
# via the Options page at runtime), not from a build-time .env file.
# This file documents the default used on first install.
DEFAULT_API_BASE_URL=http://127.0.0.1:8010
