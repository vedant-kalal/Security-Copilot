"""
Network anomaly detection (spec section 5) — build order step 8, after
the link/email agent path is proven.

`feature_engineering.py` and `isolation_forest.py` are ported-as-is
from the prior implementation and still work, but don't yet match spec
section 5.1/5.2 exactly (see each file's module docstring for what's
left). `flow_collector.py` and `tranad.py` are stubs — nothing in them
runs yet.
"""
