# Static blocklist — the router's first, cheapest check (spec section 2).
# One domain or full URL per line. Lines starting with # are ignored.
# Empty by default; add known-bad domains here as you find them.
