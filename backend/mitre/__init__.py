"""
MITRE ATT&CK technique mapping + RAG-grounded remediation (spec
sections 6/7) — build order step 11, after network anomaly detection
works. `legacy_mappings.py` is a hand-curated signal->technique dict
kept from the prior implementation as a cross-check reference, not the
real STIX-based approach. `build_index.py` and `lookup.py` are stubs.
"""
