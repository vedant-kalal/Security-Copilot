# --- Web framework -----------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Settings / validation -----------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database -----------------------------------------------------
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0


# --- Auth / security -----------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- HTTP client (threat intel) -----------------------------------------------------
httpx==0.28.1

# --- Machine learning -----------------------------------------------------
# IMPORTANT: torch is intentionally NOT listed here. Install it yourself
# FIRST, with the command matching your hardware, then run
# `pip install -r requirements.txt` for everything else. See
# docs/INSTALLATION.md ("PyTorch / GPU setup") for exact commands,
# including the NVIDIA RTX 50-series (Blackwell / CUDA 12.8) case.
#   CPU only:   pip install torch
#   NVIDIA GPU: pip install torch --index-url https://download.pytorch.org/whl/cu128
transformers==4.47.1
scikit-learn==1.6.0
numpy==2.2.1
joblib==1.4.2
pandas==2.2.3

# --- LLM / RAG (Gemini) -----------------------------------------------------
google-genai==1.2.0

# --- Logging / utils -----------------------------------------------------
python-dotenv==1.0.1

# --- Testing -----------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0
